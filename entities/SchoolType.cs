namespace CoreSchool.entities
{
    public enum SchoolType
    {
        // We can assign a number to each School Type, but it's not necessary, by default, each var has a number assigned, starting from 0.
        Kindergarten, Elementary, HighSchool
    }
}